{
  "name": "events-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:local": "wrangler d1 migrations apply events-db --local",
    "db:remote": "wrangler d1 migrations apply events-db --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260830.1",
    "typescript": "^7.0.2",
    "wrangler": "^4.127.1"
  }
}
